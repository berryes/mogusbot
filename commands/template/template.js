module.exports = {
    name: "The asd",
    arguments: 'none',
    usage: [`${process.env.PREFIX} `],
    description: "",
    type: "Fun",
    run: (client, message, args) => {
       message.reply("meleg")
}
}


